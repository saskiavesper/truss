pre-commit:
  parallel: true
  commands:
    format:
      run: mise format
      stage_fixed: true
      glob: "*.{rs}"
    lint:
      run: mise lint
      glob: "*.{rs}"
    test:
      run: mise test
      glob: "*.{rs}"

commit-msg:
  commands:
    cog:
      run: cog verify --file {1}
