pre-commit:
  parallel: true
  commands:
    fmt:
      run: cargo fmt -- --check
      stage_fixed: true
    clippy:
      run: cargo clippy --all-targets -- -D warnings

commit-msg:
  commands:
    cog:
      run: cog verify --file {1}
