pre-commit:
  parallel: true
  commands:
    format:
      run: mix format
      stage_fixed: true
    credo:
      run: mix credo --strict
    test:
      run: mix test

commit-msg:
  commands:
    cog:
      run: cog verify --file {1}
